# Dépendances & build
node_modules/
dist/

# Données locales (base sqlite, logs, captures debug, sessions) — reconstructibles
data/
*.db
*.db-*

# Secrets
.env
.env.local

# Environnement Python (weasyprint) — reconstructible
.venv/
__pycache__/

# Outillage agent local (non lié au projet) — installs reproductibles
.claude/skills/
.claude/settings.local.json
_bmad/
_bmad-output/

# Toolkit de candidature : machinerie CV/lettre personnelle (données perso,
# photo, profil). Hors périmètre du repo public.
candidature-toolkit/
